{
    "vue中虚拟DOM是什么？为什么要使用虚拟DOM?":"虚拟DOM是对真实DOM的封装，真实DOM的每次渲染都会引起页面的重绘或重排，影响浏览器的性能。虚拟DOM就是为了解决浏览器性能问题而被设计出来的，采用diff算法，每次DOM操作不会立即更新DOM树，而是遍历一次虚拟DOM，查找其中有哪些对象被更改，而后一次映射成真实DOM，从而提高性能。",
    "什么是WVVM":"分别是M（Model，模型层 ），V（View，视图层），VM（ViewModel，V与M连接的桥梁，也可以看作为控制器）。1、M：模型层，主要负责业务数据相关；2、V、",
    "":"",
    "":"",
    "":"",
}